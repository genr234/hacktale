export interface Prize {
	name: string;
	image: string;
	price: number;
}

export interface User {
	email: string;
	name: string;
	verificationStatus: string;
	hackclubId: string;
	avatarUrl: string;
	slackId: string;
	createdAt: string;
	updatedAt: string;
	currency: number;
}
